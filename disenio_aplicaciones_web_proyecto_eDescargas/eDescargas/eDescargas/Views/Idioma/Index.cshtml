@model IEnumerable<eDescargas.Models.idioma>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_ViewStart.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idioma1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idioma1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_idioma }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_idioma }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_idioma })
        </td>
    </tr>
}

</table>
